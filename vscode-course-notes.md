# VS Code 入門三小時 — 講師教案筆記

對象：有基礎電腦操作、未寫過程式的學員
時長：約 3 小時（含休息）
形式：投影 + 現場跟做（每個環節都要求學員動手，不用投影帶過）

**教學順序邏輯**：安裝後先讓學員動手做「有感」的事（開自己的資料夾、換喜歡的顏色），累積成就感後才進入偏理論的介面導覽；終端機與 Python／Git 環境確認完，才進小遊戲與個人名片網頁兩段實作收尾。

頁碼對應目前的 `jo6bj03284504t_6g4a83.pptx`（59 頁）。學員版線上講義：<https://vscode.bobhsu.com>（極簡索引，講師/審核用完整版在 <https://vscode.bobhsu.com/master.html>）。
學員下載：`course-materials.zip`（課程PPT.pptx、練習.txt、上課用ppt.pptx、card-template.html、game.py、game-extended.py）。

---

## 課前準備 Checklist

- [ ] 確認場地網路速度（安裝 VS Code + 擴充套件需要下載）
- [ ] `course-materials.zip` 已更新到最新版，且可從 vscode.bobhsu.com 正常下載解壓
- [ ] 確認 Office Viewer 類擴充套件在市集上的正確名稱與安裝方式
- [ ] 確認學員電腦的 Python 指令名稱（Windows 可能是 `py`，macOS 可能是 `python3`）
- [ ] 確認學員電腦是否已裝 Git（`git --version`）；沒裝的話準備好 `git-scm.com` 安裝步驟講解
- [ ] 準備好可用的 AI 對話工具（ChatGPT / Claude 網頁），確認學員連得到、能貼 prompt
- [ ] 確認學員機器有預設瀏覽器，且能開啟本機 `file://` 檔案（最後的名片實作要用）
- [ ] 準備一份「安裝失敗排除小抄」：
  - Windows 無系統管理員權限 → User Installer（官網預設版即是）
  - Windows SmartScreen 攔下 → 其他資訊 → 仍要執行
  - 公司 / 學校網路擋下載 → 隨身碟備用安裝包
  - macOS 第一次開跳「來自網路下載」→ 直接打開

---

## P.3–8｜安裝與初次啟動

**目標**：學員能獨立完成安裝，先看一眼介面（不深入解釋）。

1. 下載與安裝
   - 官網下載對應版本，強調這是編輯器不是瀏覽器
   - Windows：下載 `.exe`，安裝時建議勾選「加入右鍵選單」「加入 PATH」
   - macOS：下載 `.zip`，解壓縮後拖入應用程式資料夾
   - 常見卡點：學校/公司電腦沒有系統管理員權限 → 使用者層級安裝版
   - **現場巡場，不要用投影帶過這一步**——最容易個別卡住

2. 初次啟動一覽
   - 語言暫時保持英文，介面認識與中文化留到後面模組
   - 示範一次 `Ctrl+Shift+P`（Mac `Cmd+Shift+P`）開命令面板，讓學員有印象即可，不深入教

---

## P.9–13｜開啟資料夾＝專案，瀏覽自己的檔案

**核心觀念**：VS Code 沒有「新建專案」按鈕，**一個資料夾就是一個專案**。

- 用學員剛下載解壓的課程資料夾練習，比新建空資料夾更有感
- `File → Open Folder` → 左側 Explorer 出現樹狀清單——第一次看到「一堆檔案變成可操作的樹狀清單」

**懸念鉤子（P.13「誒!為啥你的這麼紅????」）**：這時有人的檔名可能已經是紅色（未儲存）或彩色（Git 追蹤到的變更）。故意先不解釋，口頭點一句「等一下會揭曉」，**在 Git 模組（P.57）明確回收這個懸念**：「還記得一開始檔名變紅嗎？那就是 Git 在追蹤你的修改。」

---

## P.14–17｜個性化：換主題

- 命令面板 → `Preferences: Color Theme`（方向鍵即時預覽）
- 命令面板 → `Preferences: File Icon Theme`
- 可額外安裝主題套件（如 One Dark Pro）

**帶教技巧**：這是最有成就感的一步，鼓勵大家展示自己選的主題，順便複習命令面板用法。名片實作換主題色時會回扣這一步（「剛才換的是 VS Code 的顏色，現在換的是自己作品的顏色」）。

---

## P.18–26｜在 VS Code 裡打開 txt / PPT 文件

**節奏**：故意先讓學員點 `上課用ppt.pptx`（P.20）→ 打不開 / 亂碼（P.21「誒!為啥我的ppt打不開」）→ 「為什麼？」→ 裝擴充套件解決（P.22-25）→ 回頭再打開一次成功（P.26）。

1. 打開 `練習.txt`（P.19）：直接點開看內容
2. 打開 `上課用ppt.pptx`（P.20）：預設打不開或亂碼，這是正常的，VS Code 不認識 PPT 格式
3. 裝 Office Viewer 類套件（P.22-25）：Extensions 面板搜尋安裝
4. 再打開一次 `上課用ppt.pptx`（P.26）：可以直接分頁翻頁預覽，不用另外開 PowerPoint

**帶學員做**：每個人自己打開一次，建立「VS Code 也能看我平常的檔案」的直覺。

---

## P.27–30｜建立與儲存檔案、開啟 Auto Save

1. 右鍵 Explorer → New File，練習新建一個 `練習.txt`（P.27）
2. 分頁圓點 = 未儲存；`Ctrl+S` 存檔（P.28）
3. **節奏**：「好煩，不想一直按存檔」（P.29）→ `File → Auto Save` 打勾（P.30）

**常見誤解要糾正**：學員常以為要「存專案」，其實只存個別檔案，資料夾只是視窗顯示範圍。

**小提醒（口頭）**：Auto Save 適合今天的練習；之後寫大專案、或程式會「存檔即重跑」時，可能會想關掉。開了 Auto Save 後，後面步驟裡「記得 Ctrl+S」可以省略，直接提醒學員「改完就自動存了」。

---

## P.31｜繁體中文化（可選）

- 介面已玩過一輪、顏色也換好，讓學員**自行決定**是否切中文
- 命令面板 → `Configure Display Language` → 安裝 `Chinese (Traditional) Language Pack` → 重啟
- 投影一律用中文介面示範，學員保留英文也 OK

---

## P.32–37｜介面五大分區

**目標**：把前面摸過的操作（Explorer、命令面板、主題設定）正式「掛回」對應的介面分區名稱，用「房間」比喻：

- Activity Bar（最窄那排圖示）= 切換面板的按鈕
- 側邊欄 = 檔案總管 / 搜尋 / 原始碼控制 / 擴充套件——剛才開資料夾、裝 Office 套件都在這裡操作
- 編輯區 = 打字、看 PPT 的地方，可分割
- 狀態列（底部）= 語言、編碼、游標位置、Git 分支
- 右上角工具列 = 分割編輯器、開終端機、切版面

**這段是全課唯一沒有「帶學員做」的純講解區塊**，且排在休息前——若前面環節超時擠壓到這裡，容易變成投影唸過去。**若要壓縮時間，這裡是優先犧牲對象**（學員已經用過這些功能，只是還沒有名稱），可以只留 Activity Bar 和側邊欄兩項，選單列細節（File/View/Run）直接跳過。

☕ **休息 10 分鐘（P.38）**

---

## P.39｜擴充套件：安裝 Python

- Extensions 面板搜 `Python`，裝 **Microsoft 官方**那個
- 說明它讓 VS Code 多了「執行、自動完成、除錯 Python」的能力
- **「等下要寫一支小程式來執行」——不是爬蟲**

---

## P.40–41｜認識整合終端機

**為什麼要教**：小遊戲和 Git 都要用終端機，但學員從沒看過。

- `Ctrl+\`` 打開／收起，或 `Terminal → New Terminal`
- 強調：它一打開就站在你的專案資料夾裡，不用自己 `cd`
- 示範 `dir`（mac `ls`）
- **降低恐懼**：打錯頂多紅字，不會弄壞電腦；看不懂的紅字整段貼給 AI 問

---

## P.42｜確認 Python 是否安裝成功

**為什麼要教**：不先確認，學員在下一步按執行鍵會報錯（找不到 python / 沒選直譯器），現場一次卡一片。

- 終端機打 `python --version`，有版本號就 OK
- 沒反應 / 紅字 → 改試 `py --version` 或 `python3 --version`，哪個有回應就用哪個
- 若跳「Select Interpreter」提示 → 選系統的 Python
- **講師巡場，確認每個人都看到版本號才往下**

---

## P.43–50｜實作：用 AI 做一個終端機小遊戲

**做法**：不自己從頭寫，改成「AI 寫、學員看懂 + 貼上 + 執行」——今天的小遊戲教法**以此為準**，不要求學員逐行手打固定程式碼。

1. AI 對話貼 prompt（P.43）：
   > 請幫我寫一個國小數學的問答挑戰 Python 檔，可以在終端機畫面做答題
2. 新建 `game.py`（P.44），把 AI 給的程式碼整段複製貼上、存檔（P.45-47）
3. 點右上角 ▶（Run Python File），或終端機 `python game.py`（P.48）
4. 在終端機輸入答案、按 Enter（P.49）
5. 中途結束：點終端機，`Ctrl+C`（P.50）

**帶學員做**：請每個人自己跑一次，答對答錯都好，重點是看到程式有回應。

**備援**：課程資料夾裡已經附了一份能跑的 `game.py` 和進階版 `game-extended.py`（五題、四則運算、成績結算）——AI 一時生不出能跑的程式，或想先玩玩看，隨時可以打開資料夾裡現成的這兩份。**不要求當場打完**，這兩份是備援不是教學主線。

---

## P.51–56｜實作：做一張自己的個人名片網頁

**目標**：把今天學過的技巧（存檔、多游標、搜尋取代）一次用在同一個檔案上，最後在瀏覽器看到自己的成果。**不需要網路、不需要安裝任何東西**——刻意設計成不可能失敗，因為它是學員帶走的最後印象。

1. 找到模板，用瀏覽器打開它（P.51-53）
   - 回到一開始打開的課程資料夾，裡面有一個 `card-template.html`
   - 在 VS Code 的 Explorer 對 `card-template.html` 右鍵 → `Copy Path`
   - 打開 Chrome，把剛複製的路徑貼到網址列，按 Enter
   - **教學用意**：先看到「這是一張真的名片」，學員才知道自己在改什麼。照「先操作、再解釋」，這時候不先講 HTML 是什麼

2. 三個編輯技巧，一次用上（P.54）
   - `Ctrl+D` 換名字：游標點在任一個 `你的名字` 上，連按選起全部 3 處，直接打自己的名字。順手把 `.avatar` 裡的「名」改成自己姓名的第一個字。按過頭就 `Ctrl+U` 退回
   - `Alt`+點擊 改三個技能：按住 `Alt`（Mac：`Option`）依序點在三個技能標籤上，三個游標同時打字
     - **常見誤解**：多游標不是「複製貼上三次」，是三個游標同時打同樣的字；要打不一樣的內容就得一個一個改——這正是它的限制
   - `Ctrl+H` 換主題色：找 `#3D5A80`（連 `#` 一起輸入），確認「找到 5 筆」後全部取代。模板註解裡有四組建議色
     - **帶教技巧**：呼應「個性化：換主題」模組——「剛才你換的是 VS Code 的顏色，現在換的是自己作品的顏色」

3. 填完欄位，回瀏覽器看成果（P.55）
   - 自我介紹、身分、Email、城市直接打字改，`Ctrl+S` 存檔（如果有開 Auto Save 就不用按存檔）
   - 切回瀏覽器按 `F5` 重新整理
   - **帶學員做**：鼓勵學員轉螢幕給旁邊的人看，比照換主題那段的分享節奏

**收尾一句話**：你剛剛不是「學會寫網頁」，是學會了用 VS Code 快速改一份檔案——這件事對任何檔案都適用。

**排除狀況**：
- 中文亂碼 → 確認模板的 `<meta charset="utf-8">` 還在，狀態列右下角應顯示 UTF-8
- 貼上網址列沒反應 → 確認複製的是 `Copy Path` 不是 `Copy Relative Path`；或把檔案直接拖進已開啟的瀏覽器視窗
- 改壞版面 → `Ctrl+Z` 可救，資料夾裡的原始檔也隨時可以重新複製一份出來對照

---

## P.57｜Git 基礎操作

**鉤子回收**：「還記得 P.13 那個紅紅的檔名嗎？那就是 Git 在追蹤你的修改。」

- **前提要先講**：VS Code 的「原始碼控制」面板只是介面，實際執行的是電腦上另外裝的 Git 本身，VS Code 不內建它
- 終端機 `git --version` 確認是否已裝——有版本號就跳過安裝；沒反應就去 `git-scm.com` 下載，裝完要**重啟 VS Code**
- **常見卡點**：沒裝 Git 時左側原始碼控制圖示會顯示「需要安裝 Git」的提示——**上課前務必在學員機型測過一次**，比想像中容易卡住
- 終端機 `git init`
- 編輯檔案後左側原始碼控制圖示會顯示變更數
- 輸入 commit 訊息，`Ctrl+Enter` 完成 commit
- **不深入教指令**，只建立「有版本控制這回事」的概念，用面板點按即可（避免學員在終端機打 `git commit` 因沒 `git add` 而失敗）

---

## P.58｜課後自我檢查清單（可發給學員）

- [ ] 能獨立安裝 VS Code，並完成第一次啟動
- [ ] 能開啟資料夾當專案，用擴充套件直接瀏覽 txt / PPT 文件
- [ ] 能切換色彩主題，依個人習慣決定是否切換為繁體中文
- [ ] 能講出介面五大分區的用途
- [ ] 能安裝 Python 擴充套件，開啟終端機並確認 python 指令可用
- [ ] 能用 AI 產生程式碼、貼回 VS Code 執行一個小遊戲
- [ ] 能用多游標、搜尋取代完成一份自己的作品（個人名片網頁），並在瀏覽器打開它

---

## P.59｜如何用當前的 AI 去做開發

**現況**：這頁目前只有一句引言「假設……我不想要再跟 AI 對話複製貼上程式碼……」，是一個開放式的延伸鉤子，**投影片本身沒有寫下文，全靠講師現場口頭發揮或當場展示**。

**建議講師準備**：至少口頭補完這頁的邏輯閉環——呼應 P.43-50 學員剛體驗過的「複製貼上 AI 生成的程式碼」，點出下一步可能的方向（例如 AI 直接在編輯器裡讀寫檔案、不用手動複製貼上），當成「今天學的只是起點」的收尾金句，留一個明確的延伸學習方向給學員，不要臨場無準備地帶過。

---

## 備用時間調整建議

- **若整體超時**：
  - P.18–26 Office 擴充套件示範可只做 PPT 或 Word 一種，不必兩者都做
  - P.32–37 介面五大分區可壓縮成只留 Activity Bar 和側邊欄，選單列細節跳過
- **若整體提前**：
  - P.51–56 名片實作可延長，讓學員再改 `border-radius`、把 `.avatar` 換成 emoji、加第四個技能標籤
  - 展示 `game-extended.py`，說明「同一個想法可以長成什麼樣子」
- **某學員卡在 Python / Git 環境**：先請他跑資料夾裡現成的 `game.py`，環境問題課後個別處理，不卡全班
