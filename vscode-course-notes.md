# VS Code 入門三小時 — 講師教案筆記

對象：有基礎電腦操作、未寫過程式的學員
時長：3 小時（含 10 分鐘休息）
形式：投影 + 現場跟做（每個環節都要求學員動手，不用投影帶過）

**教學順序邏輯**：安裝後先讓學員動手做「有感」的事（開自己的檔案、換喜歡的顏色），累積成就感後才進入偏理論的介面導覽，最後兩段實作各給一次成就感——先在終端機寫一支小程式並執行，再做一張自己的個人名片網頁收尾。

---

## 課前準備 Checklist

- [ ] 確認場地網路速度（安裝 VS Code + 擴充套件需要下載）
- [ ] 在課程網頁提供下載連結：課程講義 PPT / Word 文件（供學員在「檔案與專案管理」模組打開練習）
- [ ] 確認 Office Viewer 類擴充套件（by hediet 或同類）在市集上的正確名稱與安裝方式
- [ ] 確認學員電腦的 Python 指令名稱（Windows 可能是 `py`，macOS 可能是 `python3`）
- [ ] 確認學員電腦是否已裝 Git（`git --version`）；沒裝的話準備好 `git-scm.com` 安裝步驟講解
- [ ] 確認學員機器有預設瀏覽器，且能開啟本機 `file://` 檔案（最後的名片實作要用）
- [ ] 提醒 Windows 學員在檔案總管開啟「顯示副檔名」——避免名片檔存成 `card.html.txt`
- [ ] 準備一份「安裝失敗排除小抄」，常見狀況：
  - Windows 無系統管理員權限 → 改用使用者層級安裝版
  - 公司/學校網路擋下載 → 準備隨身碟備用安裝包
  - 防毒軟體誤判 → 提前加入白名單

---

## 00:00–00:20｜安裝與初次啟動

**目標**：學員能獨立完成安裝，先看一眼介面（不深入解釋）。

1. 下載安裝（15 min）
   - 官網下載對應版本，強調這是編輯器不是瀏覽器
   - Windows 安裝時勾選「加入右鍵選單」「加入 PATH」
   - **現場巡場，不要用投影帶過這一步**——最容易個別卡住

2. 初次啟動一覽（5 min）
   - 語言暫時保持英文，介面認識與中文化留到後面模組
   - 示範一次 `Ctrl+Shift+P` 開命令面板，讓學員有印象即可，不深入教

---

## 00:20–00:45｜檔案與專案管理：實際打開文件

**核心觀念**：VS Code 沒有「新建專案」按鈕，**一個資料夾就是一個專案**。這裡直接用學員自己的下載資料夾練習，比新建空資料夾更有感。

1. 開啟資料夾（10 min）
   - File → Open Folder，選取「下載」資料夾（課程講義 PPT / 文件已預先透過課程網頁提供下載）
   - 左側 Explorer 顯示該資料夾內容

2. 用 Office 擴充套件在 VS Code 裡看 PPT / Word（15 min）
   - Extensions 面板搜尋安裝 Office Viewer 類套件
   - 點開課程講義 PPT，示範可以直接在分頁裡翻頁預覽，不需要另開 PowerPoint
   - 順便示範開啟一個 `.docx` 講義文件比對
   - **帶學員做**：請每個人自己在下載資料夾裡找到今天的講義並點開看一次

3. 建立、儲存檔案小練習（5 min）
   - 右鍵 Explorer → New File，練習新建空白 `.txt`
   - `Ctrl+S` 存檔；分頁圓點＝未儲存

**常見誤解要糾正**：學員常以為要「存專案」，其實只存個別檔案，資料夾只是視窗顯示範圍。

---

## 00:45–01:05｜換主題、換字型

1. 切換色彩主題（10 min）
   - 命令面板 → `Preferences: Color Theme`（方向鍵即時預覽）
   - 命令面板 → `Preferences: File Icon Theme`
   - 可額外安裝主題套件（如 One Dark Pro）擴充選擇
   - **帶教技巧**：這是最能讓學員有成就感的一步，鼓勵大家展示自己選的主題，順便複習命令面板用法

2. 調整字型大小與字體（10 min）
   - 設定搜尋 `font size` / `font family`
   - 示範放大字型對長時間閱讀程式碼的幫助

---

## 01:05–01:15｜繁體中文化（可選）

- 介面已經玩過一輪、顏色也換好了，讓學員**自行決定**是否切換中文介面
- 命令面板 → `Configure Display Language` → 安裝 `Chinese (Traditional) Language Pack`
- **教學用意**：不強制統一語言，投影示範一律用中文介面講解，學員機器保留英文也沒問題

---

## 01:15–01:40｜介面認識

**目標**：把前面幾個模組已經摸過的操作（Explorer、命令面板、主題設定），正式「掛回」對應的介面分區名稱。

1. 五大分區導覽（15 min）— 用「房間」比喻：
   - Activity Bar（最窄那排圖示）＝ 走廊的門
   - 側邊欄 ＝ 依門打開的房間（檔案總管／搜尋／原始碼控制／擴充套件）——剛才開資料夾、裝 Office 套件都在這裡操作
   - 編輯區 ＝ 真正做事的桌子、剛才看 PPT 的地方，可分割
   - 狀態列（底部）＝ 儀表板：語言、編碼、游標位置、Git 分支
   - 右上角工具列 ＝ 分割編輯器、開終端機、切版面

2. 最上方選單列（File / Edit / View…）（10 min）
   - 跟命令面板是**兩條通往同樣功能的路**，初學者常忽略但更適合「用滑鼠慢慢找」的習慣
   - `File`：Open Folder、Save、Save As——就是剛才開下載資料夾的另一條路
   - `View`：開啟 Explorer、Terminal、Extensions 等面板，跟 Activity Bar 是同一批功能的另一入口
   - `Run`：執行目前開啟的檔案，等一下寫小程式時也能從這裡跑
   - **視窗變窄時**選單列會摺疊成「⋯」（More Actions）圖示，收進 Terminal、Help 等選項——提醒學員別以為選單不見了

☕ **01:40–01:50 休息 10 分鐘**

---

## 01:50–02:15｜編輯技巧與快捷鍵

1. 多游標與搜尋取代（12 min）
   - `Alt`/`Option` + 點擊多處同時打字
   - `Ctrl+D` 選取下一個相同字詞
   - `Ctrl+F` 找、`Ctrl+H` 取代、`Ctrl+Shift+F` 全專案搜尋

2. 快捷鍵小抄（13 min）— 見講義網頁的快捷鍵表格，現場可印出發給學員

---

## 02:15–02:25｜擴充套件：安裝 Python

- Extensions 面板（方塊圖示）安裝 `Python`（Microsoft 官方）— 等下要寫一支小程式來執行
- 示範停用 vs 解除安裝的差異
- 主題、字型、中文包已在課程一開頭裝過，這裡只需補裝實作要用的套件

---

## 02:25–02:45｜終端機、寫一支程式執行、Git 基礎

1. 內建終端機（6 min）
   - `Ctrl+\`` 開啟
   - 測試 `python --version`（或 `py` / `python3`，依課前測試結果調整）
   - `cd` 切換目錄、`dir`/`ls` 列出檔案

2. 寫一支程式並執行（8 min）

   **目標**：讓學員第一次體驗「我寫的東西跑起來了」。程式要短到每個人都能跟著打完，不是看老師打字。

   新建 `game.py`，帶學員一行一行打：
   ```python
   import random

   a = random.randint(1, 50)
   b = random.randint(1, 50)

   print(f"{a} + {b} = ?")
   answer = int(input("你的答案："))

   if answer == a + b:
       print("答對了！🎉")
   else:
       print(f"答錯囉～ 正確答案是 {a + b}")
   ```

   終端機執行：
   ```
   python game.py
   ```

   - **帶學員做**：請每個人自己跑一次，答對答錯都好，重點是看到程式有回應
   - **常見卡點**：`python` 指令名稱依 OS 而異（`py` / `python3`），依課前實測結果統一口徑
   - **行有餘力**：`game-extended.py` 是同一個想法的完整版（五題、四則運算、成績結算），給提早做完的學員看，**不要求當場打完**

3. Git 基礎（6 min）
   - **前提要先講**：VS Code 的「原始碼控制」面板只是介面，實際執行的是電腦上另外裝的 Git 本身，VS Code 不內建它
   - 終端機 `git --version` 確認是否已裝——有版本號就跳過安裝；沒反應就去 `git-scm.com` 下載，裝完要**重啟 VS Code**
   - **常見卡點**：沒裝 Git 時左側原始碼控制圖示會顯示「需要安裝 Git」的提示——**上課前務必在學員機型測過一次**，比想像中容易卡住
   - 終端機 `git init`
   - 編輯檔案後左側原始碼控制圖示會顯示變更數
   - 輸入 commit 訊息，`Ctrl+Enter` 完成 commit
   - **不深入教指令**，只建立「有版本控制這回事」的概念

---

## 02:45–03:00｜實作：做一張自己的個人名片網頁

**目標**：把今天學過的 VS Code 技巧（新建檔案、存檔、多游標、搜尋取代）一次用在同一個檔案上，最後在瀏覽器看到自己的成果。
**不需要網路、不需要安裝任何東西**——這一段刻意設計成不可能失敗，因為它是學員帶走的最後印象。

1. 建立檔案並貼上模板（4 min）
   - Explorer 右鍵 → New File，檔名**整串連 `.html` 一起打**：`card.html`
   - 從課程網頁複製模板全文，`Ctrl+V` 貼上，`Ctrl+S` 存檔
   - **帶學員做**：存檔後注意分頁圓點消失了——這就是模組 2 講過的「已儲存」
   - **常見誤解**：只打 `card` 會變成沒有副檔名的檔案，瀏覽器不認得。副檔名一定要自己打出來

2. 先看一眼成品（1 min）
   - Explorer 對 `card.html` 右鍵 → Reveal in File Explorer / Finder，用瀏覽器打開
   - **教學用意**：先看到「這是一張真的名片」，學員才知道自己在改什麼。照「先操作、再解釋」，不先講 HTML 是什麼

3. 用 `Ctrl+D` 換掉名字（3 min）
   - 游標點在任一個 `你的名字` 上，連按 `Ctrl+D` 選起全部 3 處
   - 直接打自己的名字，三處同時變更
   - 順手把 `.avatar` 裡的「名」改成自己姓名的第一個字
   - **小技巧**：按過頭就 `Ctrl+U` 退回上一個選取

4. 用 `Alt`+點擊改三個技能（2 min）
   - 按住 `Alt`（Mac：`Option`）依序點在 `技能一`／`技能二`／`技能三` 上
   - **常見誤解**：多游標不是「複製貼上三次」，是三個游標同時打同樣的字；要打不一樣的內容就得一個一個改——這正是它的限制

5. 用 `Ctrl+H` 換主題色（3 min）
   - `Ctrl+H`，找 `#3D5A80`（**連 `#` 一起輸入**），取代成喜歡的色碼，模板註解裡有四組建議色
   - 取代前先確認「找到 5 筆」的數字對不對，再按全部取代
   - **帶教技巧**：呼應 00:45——「剛才你換的是 VS Code 的顏色，現在換的是自己作品的顏色」

6. 填完剩下的欄位並存檔（1 min）
   - 自我介紹、身分、Email、城市，直接打字改，`Ctrl+S`

7. 回瀏覽器看成果（1 min）
   - 切回瀏覽器按 `F5` 重新整理
   - **帶學員做**：鼓勵轉螢幕給旁邊的人看，比照換主題那段的分享節奏

**收尾一句話**：你剛剛不是「學會寫網頁」，是學會了用 VS Code 快速改一份檔案——這件事對任何檔案都適用。

**排除狀況**：
- 中文亂碼 → 確認模板第 4 行的 `<meta charset="utf-8">` 還在，狀態列右下角應顯示 UTF-8（回扣模組 5「狀態列＝儀表板」）
- 瀏覽器不開 `file://` → 把檔案拖進已開啟的瀏覽器視窗，或右鍵 Copy Path 貼到網址列
- 改壞版面 → `Ctrl+Z` 可救，課程網頁的模板原文隨時可以重貼

---

## 課後自我檢查清單（可發給學員）

- [ ] 能獨立安裝 VS Code
- [ ] 能開啟資料夾，並用 Office 擴充套件直接瀏覽 PPT / Word 文件
- [ ] 能切換喜歡的色彩主題與字型，依個人習慣決定是否切換為繁體中文
- [ ] 能講出五大介面分區與選單列的用途，並用命令面板找到任何功能
- [ ] 能使用多游標與搜尋取代加速編輯
- [ ] 能安裝擴充套件（Python）並開啟整合終端機執行基本指令
- [ ] 能寫出一支簡單的 Python 程式並在終端機執行
- [ ] 能新建 HTML 檔案、用多游標與搜尋取代完成一份自己的作品，並在瀏覽器打開它

---

## 備用時間調整建議

- 若整體超時：Office 擴充套件示範可只示範一種檔案格式（PPT 或 Word），不必兩者都做
- 若整體提前：名片實作可延長，讓學員再改 `border-radius`、把 `.avatar` 換成 emoji、加第四個技能標籤；或展示 `game-extended.py` 說明「同一個想法可以長成什麼樣子」
